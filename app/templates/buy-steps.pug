.buy-steps.flex-row.flex-between.flex-baseline.center(ng-click="buy()" ng-class="{'flex-justify': exchangeAcct}")
  .width-25.flex-column.flex-center(ng-class="{primary: beforeStep('select-payment-medium')}" ng-hide="exchangeAcct")
    i.type-lrg.ti-user
    span(translate="ACCOUNT")

  .flex-justify(ng-hide="exchangeAcct")
    i.ti-arrow-right.type-h4

  //- card || bank
  .width-25.flex-column.flex-center(ng-class="{primary: afterStep('accept-terms') && beforeStep('isx')}")
    i.type-lrg.ti-credit-card(ng-class="{bank: (getMedium().inMedium === 'bank' || medium === 'bank') || isKYC}")
    span(translate="METHOD")

  .flex-justify
    i.ti-arrow-right.type-h4

  //- verify
  .width-25.flex-column.flex-center(ng-class="{primary: afterStep('summary') && beforeStep('trade-formatted')}")
    i.type-lrg.ti-id-badge.bank
    span(translate="VERIFY_PAYMENT")

  .flex-justify
    i.ti-arrow-right.type-h4

  .width-25.flex-column.flex-center(ng-class="{primary: onStep('trade-formatted')}")
    i.type-lrg.ti-shopping-cart
    span(translate="GET_BITCOIN")
