span
  b(translate="YOU")
  | 
  span(translate="{{ action }}")
  |   
  b(translate="{{ subject }}" tooltip="{{ address }}" ng-if="address")
  b(translate="{{ subject }}" ng-if="!address")
  span(ng-show="transaction.confirmations == 0 && transaction.from.externalAddresses")
    br
    i(translate="UNCONFIRMED_WARNING")