span
  span(ng-show="$ctrl.origin" ng-bind-html="($ctrl.origin.label || $ctrl.origin.address) | escapeHtml | highlight:$ctrl.highlight")
  span(ng-hide="$ctrl.origin.balance == null") &ensp;({{ $ctrl.origin.balance | convert }})&ensp;
  span.label.label-default(ng-show="$ctrl.origin.isWatchOnly" translate="WATCH_ONLY")
