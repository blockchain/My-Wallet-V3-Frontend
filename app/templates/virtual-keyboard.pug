table.virtual-keyboard
  tr(ng-repeat="row in (caps ? keysUpperCase : keysLowerCase)")
    td(ng-repeat="key in row")
      a(ng-click="press(key)") {{ key }}
  tr
    td(colspan=5)
      a(ng-click="toggleCaps()") Shift
    td(colspan=4)
      a(ng-click="press(' ')") Space
      