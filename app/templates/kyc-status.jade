.kyc-status.flex-row.border.pbl.ptl
  .flex-1.pll.mlm
    h1
      i.type-h4(ng-class="$ctrl.getState().i")
  .flex-4.flex-column.flex-justify.pll
    span.type-h5.mbs.mts(translate="{{$ctrl.getState().ns}}.TITLE")
    span.type-sm(translate="{{$ctrl.getState().ns}}.BODY" translate-values="{ccmax: $ctrl.getCardMax()}")
  .flex-2.flex-center.flex-end.prl.mrm
    span.blue.underline.pointer.type-sm(translate="{{$ctrl.getState().ns}}.LINK" ng-click="$ctrl.onTrigger()")
