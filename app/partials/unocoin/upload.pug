.flex-row.flex-start.flex-column-tablet
  .width-40
    .width-80
      .flex-row.flex-center.mb-10
        h4.f-20.em-500.mvn(translate="{{state.base + state.idType + '_ID'}}.TITLE")
        //- button.helper(ng-click="openHelper(state.idType + '-id-helper')" type="button" tabindex="-1")
          span ?
      .mbl
        span.f-16(translate="{{state.base + state.idType + '_ID'}}.COPY")
      ul.mbl.pln.pl-20.f-16(ng-show="state.idType === 'address'")
        li.pb-5
          span(translate="{{state.base + state.idType + '_ID'}}.OPTION1")
        li.pb-5
          span(translate="{{state.base + state.idType + '_ID'}}.OPTION2")
        li.pb-5
          span(translate="{{state.base + state.idType + '_ID'}}.OPTION3")
        li.pb-5
          span(translate="{{state.base + state.idType + '_ID'}}.OPTION4")

  .width-60
    bc-file-upload(file="state.file" id-type="state.idType" base="state.base" on-upload="prepUpload" locked="locked")
